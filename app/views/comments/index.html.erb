<%= image_tag @photo.filename, size: '300x300' %>

<hr>


<% @comments.each do |comment| %>
	<%= comment.body %><br>
	Created by: <%= comment.user.email %> <%= time_ago_in_words(comment.created_at) ago %>
	<hr>
<% end %>